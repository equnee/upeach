import { Saber } from './';
export declare class BrowserApi extends Set<string> {
    api: Saber;
    constructor(api: Saber);
    /**
     * Register a file that implements Saber's browser API
     */
    add(filepath: string): this;
    reload(): Promise<void>;
}
