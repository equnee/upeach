"use strict";
module.exports = _page => {
    const page = Object.assign(Object.assign({}, _page), { internal: Object.assign({}, (_page && _page.internal)) });
    return {
        page,
        env: {
            page
        }
    };
};
